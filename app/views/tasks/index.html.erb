<h1>Congrats!</h1>
<p>You have made it to the tasks index page.</p>
<ul>
  <% @tasks.each do |task| %>
    <li><%= "ID: #{task.id}, Title: #{task.title}, Details: #{task.details}" %></li>
  <% end %>
</ul>
